function 인풋검증기능() {
    const 이메일 = document.for(let 나의반복횟수) = 1 ; 나의반복횟수 <= 3 ; 나의반복횟수 = 나의반복횟수 + 1   ) {
    console.log("안녕하세요")
}


const 인풋창다채웠는지 = 이메일 !== ""
const 성별선택했는지 = (여자선택여부 === true) ll (남자선택여부 === true)
const 동의체크했는지 = (동의체크했는지 = 동의체크여부 === true)
if(인풋다채웠는지) (&& 성별선택했는지 && 동의체크했는지) {
    document.getElementById("가입버튼").style = "backgound-color; yellow"
    document.getElementById("가입버튼")}







    function 회원가입기능() {
            const 이메일 = document.getElementById("이메일입력창").value

           if(이메일.incluedes("@") === true) {
            alert("회원 가입을 축하합니다!")
           }else {
             alert("이메일이 올바르지 않습니다!")
           } 
} 
